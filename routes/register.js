
const express = require('express');
const router  = express.Router();

module.exports = (db) => {
  //route to redirect to register page
  router.get("/", (req, res) => {
  
  });
  return router;
};